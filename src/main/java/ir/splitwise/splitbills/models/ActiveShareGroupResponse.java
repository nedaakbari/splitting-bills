package ir.splitwise.splitbills.models;

public record ActiveShareGroupResponse(String title,
                                       double totalCost,
                                       String description) {
}
